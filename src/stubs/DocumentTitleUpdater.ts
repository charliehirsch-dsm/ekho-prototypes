/**
 * Stub for DocumentTitleUpdater.
 * Production version uses useMatches() which requires createBrowserRouter.
 * The prototype uses BrowserRouter, so we provide a no-op.
 */
export function DocumentTitleUpdater(): null {
  return null;
}
